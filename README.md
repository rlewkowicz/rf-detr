# RF-DETR, but faster
This is just rf-detr hacked up with ai workflows, to make the training faster. Sets up micro batches, compiles the model, aligns memory etc. Custom data format, but uses off the shelf native rfdetr models.